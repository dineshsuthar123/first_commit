# Compose exposes only the console on loopback. PostgreSQL/provider have no published ports.
POSTGRES_PASSWORD=local-stateproof-only
STATEPROOF_READ_ONLY=0
# Optional local operator token; never commit a real value.
STATEPROOF_TOKEN=
EVIDENCE_BUCKET=
AWS_DEFAULT_REGION=ap-south-1
